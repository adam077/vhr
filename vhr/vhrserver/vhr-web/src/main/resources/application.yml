spring:
  datasource: # DRUID是阿里巴巴开源平台上一个数据库连接池实现，它结合了C3P0、DBCP、PROXOOL等DB池的优点，同时加入了日志监控，可以很好的监控DB池连接和SQL的执行情况，可以说是针对监控而生的DB连接池(据说是目前最好的连接池,不知道速度有没有BoneCP快)。
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/vhr?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  rabbitmq: # 相关代码注释掉了，不是很想看这个
    username: guest
    password: guest
    host: 127.0.0.1
    publisher-returns: true
  redis:
    host: 127.0.0.1
    database: 0
    port: 6379
    password:
#  cache: # 就是服务内部的缓存，倒是没什么关系
#    cache-names: menus_cache
server:
  port: 8081
  compression:
    enabled: true
fastdfs: # 就是一个服务配置，不慌
  nginx:
    host: http://192.168.91.128/